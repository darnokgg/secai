Security must be embedded into every phase of the AI life cycle—from data collection and model training to deployment and ongoing monitoring—because vulnerabilities at any stage can be exploited and magnified at scale. As AI systems increasingly drive critical decisions, attackers target data, models, APIs, and infrastructure with threats such as poisoning, prompt injection, model theft, and adversarial examples. Effective defense requires not only identifying and analyzing these attack vectors, but also designing compensating controls that reduce risk even when primary safeguards fail. These controls can include robust access management, data validation, model hardening, continuous monitoring, and strong incident response processes. By combining thorough attack analysis with layered compensating controls, organizations can maintain trustworthy, resilient AI systems.

In this module, you will:
Demonstrate the importance of security in the AI life cycle
Analyze AI system attacks and utilize compensating controls

Exam Objectives
1.3 Explain the importance of security throughout the life cycle of AI.
Security is essential at every stage of the Artificial Intelligence (AI) life cycle to protect data, algorithms, and outcomes from potential threats. The AI life cycle includes phases such as data collection, model training, deployment, and ongoing maintenance, each presenting unique security challenges and requiring robust safeguards. Data security is especially critical, as unauthorized access or manipulation can compromise the integrity and reliability of AI systems. Additionally, securing the AI life cycle involves ensuring model confidentiality, monitoring for adversarial attacks, and maintaining compliance with privacy standards. Throughout this process, the human role remains vital, as experts are needed to oversee, audit, and guide security strategies to address evolving risks and ethical considerations.

Implementing security during each phase of the AI lifecycle is crucial for addressing threats like data poisoning, model theft, and adversarial manipulation before they can undermine trust or cause harm. Establishing a formal AI life cycle supports governance oversight designed to integrate security and compliance checkpoints at every stage. This approach enables organizations to clarify responsibilities, implement consistent controls, and demonstrate due diligence. Ultimately, this disciplined approach protects both sensitive data and intellectual property while maintaining model reliability and ethical integrity over time.

Artificial intelligence now plays a central role in modern cybersecurity strategy, functioning both as a defensive tool that transforms vast amounts of telemetry into timely intelligence and as a new type of digital asset requiring innovative protection methods. Managing security throughout the AI lifecycle enables organizations to leverage AI's potential while minimizing its attack surface.

The AI lifecycle consists of several distinct stages where security and risk controls should be implemented. The typical stages in an AI lifecycle include:

Design

Data Collection and Preparation
Model Development/Training
Model Deployment/Integration
Operation/Inference
Monitoring and Maintenance
The AI Lifecycle
A circular flowchart shows the A I lifecycle.
Images (c) 123rf.com.

Each phase of the AI lifecycle ensures security controls from design through maintenance phases.

Description
An arrow moves from Design to Data Collection or Preparation, then to Model Training, then to Model Deployment, then to Operation, then to Monitoring, and then back to design.

Design
The lifecycle begins with the design phase, where business value and risk appetite intersect. Here, architects outline desired outcomes such as faster incident response or automated anomaly classification, aligning them with measurable corporate objectives such as mean time to detect and reducing false positives. Incorporating security measures at this stage means performing threat modeling, identifying potential training data tampering, assessing the impact of outages on regulated services, and understanding compliance obligations that dictate data handling procedures. Clearly articulating these constraints early on can save costly re-engineering efforts later.

Data Collection and Preparation
After objectives are established, the focus shifts to data collection and preparation. Security professionals should recognize that attackers can poison data even when they cannot access it directly, making controls for data provenance, integrity, and confidentiality essential. Implementing hash-based immutability checks on raw log archives, tokenizing personally identifiable information, and utilizing other similar privacy techniques safeguards both the business and its customers. Publicly available datasets allow teams to prototype threat-detection models without exposing internal telemetry. Tools like IBM's Adversarial Robustness Toolbox make it possible to examine the dataset for outliers that could signal poisoning before training begins.

Model Development/Selection
During the model development and training phase, the emphasis shifts to code hygiene and environment hardening. Conducting dependency scanning of machine learning frameworks, ensuring reproducible builds, and creating isolated training networks helps prevent supply-chain attacks. Security professionals can enhance resilience through adversarial training, where samples are injected to verify the model's tolerance to evasion techniques. Model selection is equally crucial to ensure they align closely with organizational governance and audit needs, such as speed and explainability.

Model Deployment/Integration
Even with a trained model ready, the deployment and integration phase presents new challenges. Before going live, it's important to use a blue-green rollout or a canary release to validate the model's predictions against a reference environment. This ensures that detection rates remain consistent under production-level traffic conditions. Additionally, integrating the model into a security information and event management (SIEM) platform is crucial to establishing a feedback channel for the alerts generated by the model. This helps ensure that alerts are logged and facilitates continuous verification.

Operation/Inference
The operation or inference phase is where the AI system generates its value. Runtime monitoring watches for features that drift beyond established training distributions, spikes in inference latency that could indicate denial-of-service attacks, or unexpected calls to infrequently used elements. Automated response playbooks can isolate suspicious inputs for human review, combining the speed of AI with the judgment of security analysts.

Monitoring and Maintenance
Monitoring and maintenance complete the lifecycle. Concept drift, new attack vectors, and evolving business priorities require regular retraining and redeployment of models. Secure feedback pipelines collect false positives, analyst annotations, and post-incident reports, channeling them back to the data preparation phase. Conducting regular red-teaming of models, managing patches for underlying libraries, and periodically reassessing threat models ensures continued alignment with corporate objectives and evolving regulatory requirements, resulting in a lifecycle that is a process of continuous improvement rather than a linear process.

When each phase integrates well-defined security controls, organizations can enjoy the benefits of AI-driven speed and accuracy without incurring unmanaged risks. On the other hand, overlooking controls at any stage (such as lax access management around data lakes or unpatched inference servers) can turn a useful model into a liability. By thoughtfully managing design, data, development, deployment, operation, and monitoring, organizations can optimize their AI initiatives effectively.

Artificial intelligence systems rely heavily on the quality of the data they use. When security measures address potential risks in data collection from the beginning, they help protect the confidentiality, integrity, and provenance that downstream systems depend on to produce reliable outputs. In contrast, if malicious or poorly curated data slips through the intake process, all subsequent controls will struggle because the foundational data is untrustworthy.

The first line of defense is verifying the authenticity of data. Whether collecting logs from security appliances, scraping open-source threat intelligence, or digitizing historical records, teams should establish a clear chain of custody and incorporate tamper-evident mechanisms at each step. Authenticity checks should apply to both human-generated and machine-generated data.

The trustworthiness of data also relies on the reputation of its sources and its accuracy. Automated crawlers should maintain allow lists of pre-vetted domains, while partners should sign legal agreements that outline ownership and permitted use of data. Even open-source licensed datasets should be verified against known-bad indicators and potentially sanitized of personally identifiable information. Logging the work done to perform these checks in immutable audit trails facilitates later investigations to identify precisely when and where questionable records entered the pipeline.

Data labeling and enrichment introduce additional security concerns. For example, if labels are crowd-sourced, robust identity verification and continuous quality scoring can help prevent subtle poisoning campaigns, where adversaries mark malicious samples as benign. Internally curated labels, reviews, and statistical anomaly detection approaches reduce the likelihood of accidental misclassification. Additionally, keeping raw and labeled data versions available allows future retraining tasks to revert to the original state, especially if tampering is discovered.

Note:
Organizations should operate a model registry (an auditable system of record for model, dataset, and evaluation versions) and a formal change-control process for any AI change that affects security posture. Promotions of high-impact rules or models require documented human approvals (such as SOC manager, model owner, risk owner), execution in defined "change windows", active monitoring, and rollback plans. These controls ensure provenance, accountability, and safe operation of AI-assisted security.
Models can only produce reliable insights when the data stream feeding them is both complete and clean. During a cyberattack, adversaries may try to starve or poison a pipeline used for network analysis. For example, a denial‑of‑service (DoS) aimed at traffic collection endpoints could overwhelm them so that little or no data is sent. The resulting silence might be translated as legitimate inactivity, thereby tricking downstream analytics to redefine "normal" traffic patterns. Defensive measures such as rate-limiting inbound requests, routing traffic through redundant sensors, and exchanging cryptographically signed heartbeat messages with sequence numbers and expiry information help the ingest layer distinguish between a true lull and data that is stalled or delayed.
Common AI Threats
An illustration shows the common A I threats from the training phase to the inference phase and then from the inference phase to the deployment phase.
Images (c) 123rf.com.

An illustration of the phases for common AI threats.

Description
Attackers may also flood the pipeline with carefully crafted but bogus records that hide among real events and gradually push a model's decision threshold in their favor. To catch this tactic, reference inputs with known outputs are included in the stream. If the outcome for these known cases drifts without a legitimate business reason, the system immediately flags that the training distribution is under attack.

Trust is also established through transparency. Documenting data provenance, validation metrics, and cleansing decisions provides stakeholders with confidence that the model is defensible. This approach also aligns with the EU AI Act's data-governance and record-keeping obligations for high-risk AI, including documenting training/validation/testing datasets, their provenance, and quality controls.

Secure collection practices ensure that data is authentic, complete, and legally compliant, allowing subsequent stages such as training, deployment, and monitoring to focus on delivering value. Investing in authenticity controls and trust mechanisms provides tangible benefits throughout the entire AI lifecycle.

Artificial intelligence can analyze billions of signals at machine speed, but its effectiveness in security diminishes if it overlooks the human context it was designed to protect. A human-centric design approach prioritizes people over algorithms in every decision, ensuring that models remain accountable, transparent, and responsive to evolving threats.

When fostering human-centric AI design principles, it is necessary to clarify distinct roles in oversight processes. The model owner is responsible for the overall integrity of the AI system. The risk owner focuses on identifying and mitigating potential risks, while the steward ensures that the system aligns with ethical guidelines and best practices. Additionally, oversight must function independently from those who build the AI systems to provide an unbiased balance in management.

Dashboards can provide valuable insights into an AI system's performance characteristics and ethical alignment. Additionally, criteria for emergency-stop controls should be defined that support immediate intervention if an AI system operates outside acceptable operating parameters or poses unanticipated risks. Additionally, audit cadence should be established, such as monthly performance reviews to monitor ongoing performance characteristics, and quarterly "fairness" checks can validate that the system remains impartial and unbiased.

Human-in-the-Loop
Human-in-the-loop is a concept that emphasizes the importance of having a qualified analyst step in at crucial points during the decision-making process. This approach ensures that automated systems do not perform critical actions without some form of human oversight.

For example, an automated system identifies a domain as suspicious based on observed patterns of behavior. Without human intervention, the system immediately blocks access to the domain, which disrupts legitimate business operations. In this situation, an experienced SOC analyst possesses valuable contextual knowledge, including an understanding of the company's ongoing projects and current IT infrastructure changes. Suppose the organization is in the middle of a merger and needs to access data located on a site that is being flagged as potentially unsafe. In this case, the analyst can weigh the identified risks against established business priorities to determine whether or not to allow access or implement a temporary workaround.

An automated detection system alerts the Security Operations Center (SOC) about possible malicious activity on a server. The system may recommend quarantining the server immediately. However, the SOC analyst recognizes that the server is crucial for vendor payment processing, which occurs within a few hours. By intervening, the engineer can facilitate a more nuanced response to investigate the activity, determining its validity or severity, while keeping the server operational and avoiding a vendor payment outage.

Human Oversight
Human oversight operates at a broader level by continually monitoring the model's behavior rather than focusing on each decision. Oversight teams review performance dashboards, audit logs, and metrics to ensure that accuracy and fairness remain within agreed-upon tolerance levels. When anomalies arise, such as the model suddenly flagging a benign internal application as malicious following a software update, oversight can suspend automated actions until confidence is restored. This supervisory layer represents organizational governance and establishes a formal escalation pathway to address regulatory requirements for explainability and accountability.

Human Validation
Human validation independently samples model outputs and compares them to expectations, either through expert review or controlled adversarial testing. In a phishing detection scenario, for instance, analysts periodically input known malicious and benign emails to check that the classifier responds as expected. Validation uncovers blind spots caused by data drift, adversarial attacks, or unnoticed feature interactions, and the feedback obtained then informs model retraining and rule refinement. This practice also creates documented trails that auditors, insurers, and incident responders can review to determine whether due diligence was performed before a breach occurred.

Together, these three functions create a layered defense that balances the efficiency of machines with the judgment of humans. Keeping humans in the loop mitigates immediate operational risks. At the same time, ongoing oversight ensures long-term alignment with business and legal obligations, and rigorous validation ensures that statistical confidence translates into real-world reliability. By designing security solutions that regard human expertise as a vital component rather than an afterthought, organizations can leverage AI's speed without sacrificing the nuanced decision-making that only humans can provide.

Response Levels
The principles of human-centric AI design focus on implementing structured risk management processes to ensure responsible AI use. Generally, these processes reflect different levels of response:

1. Detect-Only: Where the system monitors for issues without taking action on its own

2. Auto-Remediate: Where the AI performs predefined actions automatically to address any identified problems

3. Human-Approve: Where risky actions require human validation before they can be performed

To prevent potential delays in the decision-making process, service level agreements (SLAs) which define expectations for process approvals should be established. Additionally, a "two-person rule" should be enforced for any critical actions that could result in substantial operational impacts and fail-safe features to handle situations where AI decision-making confidence levels decrease or if the appropriate next steps are not clear. In circumstances like these, automated processes should switch to monitor-only mode until a human can intervene. All actions requiring approvals must be documented and tamper-evident to support accountability.

Artificial intelligence can analyze billions of signals at machine speed, but its effectiveness in security diminishes if it overlooks the human context it was designed to protect. A human-centric design approach prioritizes people over algorithms in every decision, ensuring that models remain accountable, transparent, and responsive to evolving threats.

When fostering human-centric AI design principles, it is necessary to clarify distinct roles in oversight processes. The model owner is responsible for the overall integrity of the AI system. The risk owner focuses on identifying and mitigating potential risks, while the steward ensures that the system aligns with ethical guidelines and best practices. Additionally, oversight must function independently from those who build the AI systems to provide an unbiased balance in management.

Dashboards can provide valuable insights into an AI system's performance characteristics and ethical alignment. Additionally, criteria for emergency-stop controls should be defined that support immediate intervention if an AI system operates outside acceptable operating parameters or poses unanticipated risks. Additionally, audit cadence should be established, such as monthly performance reviews to monitor ongoing performance characteristics, and quarterly "fairness" checks can validate that the system remains impartial and unbiased.

Human-in-the-Loop
Human-in-the-loop is a concept that emphasizes the importance of having a qualified analyst step in at crucial points during the decision-making process. This approach ensures that automated systems do not perform critical actions without some form of human oversight.

For example, an automated system identifies a domain as suspicious based on observed patterns of behavior. Without human intervention, the system immediately blocks access to the domain, which disrupts legitimate business operations. In this situation, an experienced SOC analyst possesses valuable contextual knowledge, including an understanding of the company's ongoing projects and current IT infrastructure changes. Suppose the organization is in the middle of a merger and needs to access data located on a site that is being flagged as potentially unsafe. In this case, the analyst can weigh the identified risks against established business priorities to determine whether or not to allow access or implement a temporary workaround.

An automated detection system alerts the Security Operations Center (SOC) about possible malicious activity on a server. The system may recommend quarantining the server immediately. However, the SOC analyst recognizes that the server is crucial for vendor payment processing, which occurs within a few hours. By intervening, the engineer can facilitate a more nuanced response to investigate the activity, determining its validity or severity, while keeping the server operational and avoiding a vendor payment outage.

Human Oversight
Human oversight operates at a broader level by continually monitoring the model's behavior rather than focusing on each decision. Oversight teams review performance dashboards, audit logs, and metrics to ensure that accuracy and fairness remain within agreed-upon tolerance levels. When anomalies arise, such as the model suddenly flagging a benign internal application as malicious following a software update, oversight can suspend automated actions until confidence is restored. This supervisory layer represents organizational governance and establishes a formal escalation pathway to address regulatory requirements for explainability and accountability.

Human Validation
Human validation independently samples model outputs and compares them to expectations, either through expert review or controlled adversarial testing. In a phishing detection scenario, for instance, analysts periodically input known malicious and benign emails to check that the classifier responds as expected. Validation uncovers blind spots caused by data drift, adversarial attacks, or unnoticed feature interactions, and the feedback obtained then informs model retraining and rule refinement. This practice also creates documented trails that auditors, insurers, and incident responders can review to determine whether due diligence was performed before a breach occurred.

Together, these three functions create a layered defense that balances the efficiency of machines with the judgment of humans. Keeping humans in the loop mitigates immediate operational risks. At the same time, ongoing oversight ensures long-term alignment with business and legal obligations, and rigorous validation ensures that statistical confidence translates into real-world reliability. By designing security solutions that regard human expertise as a vital component rather than an afterthought, organizations can leverage AI's speed without sacrificing the nuanced decision-making that only humans can provide.

Response Levels
The principles of human-centric AI design focus on implementing structured risk management processes to ensure responsible AI use. Generally, these processes reflect different levels of response:

1. Detect-Only: Where the system monitors for issues without taking action on its own

2. Auto-Remediate: Where the AI performs predefined actions automatically to address any identified problems

3. Human-Approve: Where risky actions require human validation before they can be performed

To prevent potential delays in the decision-making process, service level agreements (SLAs) which define expectations for process approvals should be established. Additionally, a "two-person rule" should be enforced for any critical actions that could result in substantial operational impacts and fail-safe features to handle situations where AI decision-making confidence levels decrease or if the appropriate next steps are not clear. In circumstances like these, automated processes should switch to monitor-only mode until a human can intervene. All actions requiring approvals must be documented and tamper-evident to support accountability.

Question 1

Flag for Review

Check Answer
An AI-based intrusion detection system is configured to automatically block suspicious domains. The oversight team notices on their performance dashboard that false-positive rates have increased, and several internal tools were briefly blocked.

They decide to temporarily disable automatic blocking while they investigate.

Which practice are they primarily demonstrating?

answer

Real-time data ingestion


Behavioral clustering


Zero-touch deployment


Human oversight

Correct Answer:
Correct
Explanation
Human oversight is the correct answer. Human oversight is shown when people monitor model performance and step in to adjust or pause automated actions based on error trends and business impact. Disabling automatic blocking while investigating the false positives is a direct example of this supervisory role.

Real-time data ingestion is incorrect. Real-time data ingestion refers to how quickly data is fed into the system, not to the act of reviewing results and changing automation settings in response to observed problems.

Zero-touch deployment is incorrect. Zero-touch deployment emphasizes minimal human involvement in rolling out changes. In this case, the team is actively intervening, which is the opposite of a hands-off approach.

Behavioral clustering is incorrect. Behavioral clustering is a modeling technique for grouping similar activities. It does not describe the process of humans watching performance metrics and deciding to modify or halt automated enforcement.

Question 2

Flag for Review

Check Answer
A security team deploys an AI-based email filter to classify incoming messages as malicious or safe. After several weeks, users report that some legitimate vendor invoices are being quarantined, while a few suspicious messages are still reaching inboxes.

The team wants to improve the system's reliability and create an auditable record that it is working as intended.

Which approach BEST applies the appropriate human role to address this issue?

answer

Human validation

Correct Answer:
Correct

Human-in-the-loop

Incorrect answer:
Incorrect

Human oversight


Fully autonomous operation

Explanation
Human validation is the correct answer. Human validation fits this scenario because analysts test and review sample outputs against known malicious and benign emails, then document the results. This directly checks whether the model's classifications match expectations and supports tuning and audit needs.

Human-in-the-loop is incorrect. Human-in-the-loop is about inserting people into specific operational decisions in real time, not systematically testing model outputs against ground truth. It does not directly provide the structured checking and evidence the team needs.

Human oversight is incorrect. Human oversight focuses on supervising overall system behavior and trends. While useful, it does not necessarily require detailed checking of individual classifications against known samples, which is what this problem calls for.

Fully autonomous operation is incorrect. Fully autonomous operation removes human checks entirely, allowing misclassifications to persist and providing no documented evidence that outputs were reviewed or validated.

Question 3

Flag for Review

Check Answer
A security engineer is designing a log collection pipeline for an AI-driven threat detection system.

The logs will come from security appliances, a third-party threat intelligence feed, and a new web crawler that scans external sites for indicators of compromise.

Which approach BEST applies authenticity checks to reduce the risk of poisoned or tampered data entering the training pipeline?

answer

Normalize all logs into a common schema, automatically label suspicious entries with a heuristic engine, and discard any records that cannot be parsed successfully.

Incorrect answer:
Incorrect

Require digital signatures from log sources, maintain an auditable chain-of-custody for ingested records, and enforce allow lists of vetted domains for the crawler before logs are accepted for use.

Correct Answer:
Correct

Anonymize all personally identifiable information in the logs, aggregate low-volume sources into summary statistics, and compress the resulting datasets for efficient long-term storage.


Store all incoming logs in an encrypted data lake with strict role-based access control and daily automated backups before they are used for model training.

Explanation
Requiring digital signatures from log sources, maintaining an auditable chain-of-custody for ingested records, and enforcing allow lists of vetted domains for the crawler before logs are accepted for use is the correct answer.This directly applies authenticity controls: signatures verify source and integrity, chain-of-custody tracks handling, and allow lists ensure the crawler pulls data from vetted, trusted domains.

Storing all incoming logs in an encrypted data lake is incorrect. Encryption, access control, and backups protect stored data but do not confirm that the data was genuine or untampered when collected.

Normalizing all logs into a common schema is incorrect. Normalization and heuristic labeling improve structure and analysis but do not verify data origin or detect authentic-looking but poisoned records.

Anonymizing all personally identifiable information in the logs, aggregating low-volume sources, is incorrect. Anonymization, aggregation, and compression support privacy and efficiency, not authenticity; they do not validate that the data came from legitimate, untampered sources.

Question 4

Flag for Review

Check Answer
How does crowd‑sourced data labeling affect the trustworthiness of datasets for security‑focused AI systems?

answer

It can improve trustworthiness by relying on majority voting so that common labeling patterns are treated as the most accurate.

Incorrect answer:
Incorrect

It can enhance trustworthiness by giving each contributor access to the full dataset so they can better understand the context before labeling.


It can be made more trustworthy by enforcing strong identity verification and continuous quality scoring for labelers.

Correct Answer:
Correct

It can support trustworthiness by accepting labels from any contributor as long as they follow detailed written instructions.

Explanation
It can be made more trustworthy by enforcing strong identity verification and continuous quality scoring for labelers is correct. These controls manage who labels the data and how well they perform over time, reducing malicious or low‑quality contributions in security‑sensitive datasets.

It can improve trustworthiness by relying on majority voting is incorrect. Majority voting can reduce random errors, but if most labelers are unskilled or biased, the majority can still be wrong, especially without reliability checks.

It can support trustworthiness by accepting labels from any contributor is incorrect. Clear instructions alone do not ensure compliance or honesty; identity and quality controls are still required.

It can enhance trustworthiness by giving each contributor access to the full dataset is incorrect. Extra context doesn't guarantee accuracy and increases sensitive data exposure.


Question 5

Flag for Review

Check Answer
Which defensive measure best helps an AI‑driven network monitoring pipeline distinguish between a real lull in traffic and a disruption caused by an attack on collection endpoints?

answer

Randomly dropping a portion of incoming events to keep the pipeline from becoming overloaded.

Incorrect answer:
Incorrect

Exchanging cryptographically signed heartbeat messages with sequence numbers and expiry information.

Correct Answer:
Correct

Disabling redundant sensors to simplify the architecture and reduce noise in the data feed.


Increasing the batch size used for model inference during periods of low network activity.

Explanation
Exchanging cryptographically signed heartbeat messages with sequence numbers and expiry information is the correct answer. Signed heartbeat messages with sequence numbers and expiry give the ingest layer a way to confirm that collectors are alive and sending data on time. If heartbeats stop or arrive out of order, the system can infer a collection problem or attack rather than assuming that traffic has simply gone quiet.

Increasing the batch size used for model inference during periods of low network activity is incorrect. Changing batch size affects performance but does not verify whether data is actually flowing. Larger batches during low activity would not reveal if collectors are under attack or silently failing, so this does not reliably distinguish real lulls from disruptions.

Randomly dropping a portion of incoming events to keep the pipeline from becoming overloaded is incorrect. Randomly dropping incoming events intentionally reduces visibility. While it might reduce load, it makes it harder to detect subtle disruptions or poisoning attempts, since missing data could be caused by the random policy instead of an adversary.

Disabling redundant sensors to simplify the architecture and reduce noise in the data feed is incorrect. Turning off redundant sensors removes an important resilience layer. Redundancy helps confirm whether gaps are localized or systemic; disabling it makes it more difficult to determine if a lull is genuine or caused by an attack on specific collection points.

Question 6

Flag for Review

Check Answer
A detection system is running in production and occasionally receives suspicious inputs that might be part of an evasion attempt.

Which practice BEST balances the speed of automation with human judgment during this phase?

answer

Ignoring rare or unusual inputs to keep throughput high and avoid slowing down automated processing.

Incorrect answer:
Incorrect

Allowing the model to immediately block any traffic that slightly deviates from the original training distribution.


Using automated playbooks to isolate suspicious inputs for human review before taking high-impact actions.

Correct Answer:
Correct

Sending all model outputs directly to an archive without review to preserve a pure record of inference behavior.

Explanation
Using automated playbooks to isolate suspicious inputs for human review before taking high-impact actions is the correct answer. Automated playbooks can react quickly by quarantining or flagging questionable activity, while analysts decide on actions that might disrupt operations. This approach leverages machine speed for initial containment but keeps humans in control of risky decisions during live inference.

Allowing the model to immediately block any traffic that slightly deviates from the original training distribution is incorrect. Automatically blocking all slight deviations from training data can produce excessive false positives and interrupt legitimate activity. Not all deviations are malicious, so strict automatic blocking without context or review is too rigid for safe operation.

Ignoring rare or unusual inputs to keep throughput high and avoid slowing down automated processing is incorrect. Rare or unusual inputs are often where attackers hide, especially when trying to evade learned patterns. Ignoring them improves throughput at the cost of missing precisely the kinds of events that runtime monitoring and response should focus on.

Sending all model outputs directly to an archive without review to preserve a pure record of inference behavior is incorrect. Archiving outputs without active review or response eliminates the operational value of real-time detection. While historical records can support later analysis, they do not help contain threats or guide decisions in the moment, which is the core concern during the inference phase.

Question 7

Flag for Review

Check Answer
Which phase of the AI life cycle focuses on defining desired outcomes and aligning them with corporate objectives and risk appetite?

answer

Model Deployment/Integration

Incorrect answer:
Incorrect

Design

Correct Answer:
Correct

Monitoring and Maintenance


Data Collection and Preparation

Explanation
Design is the correct answer. This phase defines desired outcomes, aligns them with business goals and risk appetite, and identifies security and compliance requirements through activities like threat modeling.

Data Collection and Preparation is incorrect. This phase focuses on gathering and preparing data, including protecting integrity, provenance, and confidentiality, but assumes objectives and risk posture are already defined.

Model Deployment/Integration is incorrect. This phase is about rolling out the trained model (for example, via canary releases) and integrating it with existing systems, not about initially defining business value and risk appetite.

Monitoring and Maintenance is incorrect. This phase occurs after deployment and operation, focusing on retraining, patching, and continuous improvement, not on setting initial corporate alignment.

Question 8

Flag for Review

Check Answer
A security team is configuring an AI-based detection system. For high-risk actions, they want the AI to suggest a response but require an analyst to approve it before it is executed.

Which response level are they primarily using for these high-risk actions?

answer

Detect-Only

Incorrect answer:
Incorrect

Human-Approve

Correct Answer:
Correct

Auto-Remediate


Continuous Monitoring

Explanation
Human-Approve is the correct answer. Human-Approve is when the AI proposes what to do and a qualified person must review and authorize that action before it runs, which fits the described high-risk scenario.

Detect-Only is incorrect. Detect-Only means the system raises alerts but never takes action. It does not add a formal step where a person must authorize a specific response.

Auto-Remediate is incorrect. Auto-Remediate lets the AI carry out predefined responses on its own, prioritizing speed over mandatory human review for each risky action.

Continuous Monitoring is incorrect. Continuous Monitoring describes constant observation and data collection, but it says nothing about whether actions require human approval before execution.

Question 9

Flag for Review

Check Answer
Why might a security team choose to use publicly available datasets when designing an AI-based threat detection model?

answer

To prototype detection approaches without exposing sensitive internal telemetry during early experimentation.

Correct Answer:
Correct

To eliminate the need for data governance controls, since the data is already open and unrestricted.


To fully replace internal logs so no organization-specific data is ever needed for training.


To guarantee that the resulting model will be immune to new and previously unseen attack techniques.

Explanation
To prototype detection approaches without exposing sensitive internal telemetry during early experimentation is the correct answer. Public datasets let teams test model ideas and feature choices without using proprietary logs. That reduces confidentiality and privacy risks during early experimentation while still providing realistic examples.

To fully replace internal logs so no organization-specific data is ever needed for training is incorrect. Public datasets cannot capture the unique traffic patterns, systems, and user behaviors of a specific organization. Effective models still need internal data to learn local baselines and relevant threats.

To eliminate the need for data governance controls, since the data is already open and unrestricted is incorrect. Public data still needs governance. Teams must consider licensing, potential personal data, and quality issues, and may need to sanitize or filter the data before use.

To guarantee that the resulting model will be immune to new and previously unseen attack techniques is incorrect. No dataset can provide immunity to future attacks. Public datasets help with known patterns, but models must be updated and revalidated as attackers evolve their techniques.

Question 10

Flag for Review

Check Answer
During model development for a security analytics system, why is it important to consider model selection in addition to training quality?

answer

To avoid having to perform any dependency scanning on machine learning libraries.

Incorrect answer:
Incorrect

To ensure that once trained, the model will never require retraining or performance reviews, even if attack patterns evolve over time.


To ensure the model aligns with governance needs such as speed, explainability, and auditability.

Correct Answer:
Correct

To guarantee the model can be deployed without any network segmentation or access control.

Explanation
To ensure the model aligns with governance needs such as speed, explainability, and auditability is the correct answer. Model choice affects how quickly it responds, how easily analysts can interpret its decisions, and how well its behavior can be reviewed for compliance and investigations, not just its raw accuracy.

To guarantee the model can be deployed without any network segmentation is incorrect. Model selection does not remove the need for infrastructure security. Network segmentation and access controls are required regardless of which model architecture is chosen.

To avoid having to perform any dependency scanning on machine learning libraries is incorrect. Dependency scanning is still necessary to detect vulnerable or malicious libraries. Even a well-chosen model relies on code that must be checked for supply-chain risks.

To ensure that once trained, the model will never require retraining or performance reviews is incorrect. Threats and data patterns change. Any model, regardless of how it was selected, needs periodic evaluation and retraining to stay effective and secure.

Exam Objectives
2.6 Given a scenario, analyze the evidence of an attack and suggest compensating controls for AI systems.
As artificial intelligence becomes more deeply embedded in modern digital infrastructure, understanding how to analyze attacks on AI systems and implement compensating controls is essential. AI systems introduce unique vulnerabilities, such as prompt injection, data poisoning, and model manipulation, which can undermine both their functionality and integrity. Addressing these risks requires not only identifying the techniques used to exploit AI systems but also applying compensating controls such as prompt firewalls, access restrictions, and output validation to mitigate risk and support resilient operations.

Cybersecurity programs must defend two interdependent areas: the traditional enterprise environment and the AI systems that now read, write, and act within it. An effective way to manage this is to anchor analysis using a standard catalog of adversary behaviors and then connect each behavior to controls that span AI system design, training, deployment, and operations. MITRE ATLAS is a popular tool because it translates real attacks on machine learning systems into techniques security practitioners already understand. When combined with guidance such as the NIST AI Risk Management Framework, this approach helps organizations decide which risks to accept, which to mitigate, and how to verify that mitigations work as required.

Prompt injection, jailbreaking, and other forms of guardrail circumvention are common and attempt to override system instructions by inserting commands through user input, documents, emails, or web pages. Typical signs of these attacks include models revealing hidden configuration details, ignoring guardrail policies, or performing unintended actions. Effective defenses for these attacks, like other types of attacks before the era of AI, are layered and predictable. Examples include treating external text as untrusted, clearly delimiting and encoding anything the model reads, constraining which tools the model may call, requiring explicit human approval for sensitive actions, and recording every tool invocation. Resources such as OWASP's LLM Top 10 and specific vendor product guidance from OpenAI, Microsoft, and others can be valuable sources of information when deciding where to filter, where to constrain, and where to implement confirmation-type controls.

Poisoning attacks shift the focus "upstream" by polluting training data so the model learns from a distorted perspective. With model poisoning, attackers tamper with gradients or weights used during training or in the supply chain resources used for training to influence outputs or potentially embed instructions that result in backdoor access to model data, code, or the platforms they run on. Practical compensating controls include ensuring strong data lineage validation, creating reproducible training pipelines, automated similarity and drift checks on incoming data, and identifying small canary record entries that can be evaluated during each training stage. Fortunately, MITRE ATLAS provides realistic techniques that teams can utilize to leverage the benefits these controls represent.
Input manipulation attacks focus on subtly modifying images, audio, and other similar data so that humans cannot detect the changes despite producing tangible impacts on model outcomes. Because filtering alone is imperfect, critical input processing workflows must include checks. For example, allowing a model to draft an email but reserving send privileges for humans, or to let an AI agent suggest a ticket update but gating the changes behind a human-centric review and approval process. These types of controls reflect human-in-the-loop and segmentation controls for workflows that represent high levels of risk.
Hallucinations, which describe when a model generates completely false but very believable output, become a big problem when other systems act on hallucinated model output as if it were fact. A hallucinated Application Programming Interface (API) endpoint or command, for example, can cause processes to fail or result in even more severe issues such as widespread outages, data breaches, or data corruption. Separating model output generation processes from execution processes can reduce this type of risk. Systems should validate model outputs using cross-model checks or human review processes to validate their accuracy. OWASP provides guidance that translates these concepts into developer-friendly examples such as sanitizing tool arguments, isolating retrieval processes from prompting, and validating outputs before any further actions occur.
Bias describes when automated systems, such as pipelines designed to gather data from public sources, are intentionally manipulated. For example, the material may include misleading content that is incorporated into a training set or when prompts are carefully crafted to produce outputs loaded with harmful stereotypes. To effectively combat bias, organizations should use only carefully curated datasets that have clear sources and documented histories to ensure the data is reliable and includes healthy and balanced perspectives. Additionally, periodic evaluations of data can validate that it is free from bias. In support of these objectives, model cards provide transparency about a model's capabilities, limitations, and the types of data it has been trained on. When things don't go according to plan, escalation paths define the incident response protocols for effectively addressing unexpected results.
Poorly controlled AI assistants that read inboxes, browse knowledge bases, or otherwise mimic the use of software by human users can be tricked into exfiltrating data or performing malicious actions. Least-privilege permissions can help prevent abuse, and audit controls support creating timelines of all actions.
Manipulating application integrations refers to maliciously altering or exploiting the connections between AI systems and external applications to compromise their functionality or outputs. Attackers may inject malicious data, modify API calls, or tamper with integration points to manipulate AI model behavior, bypass security, or extract sensitive information. For example, altering input data fed through an API can cause a model to produce incorrect predictions or reveal training data. Attacks like these exploit the trust in the connections established between systems. Defending against the manipulation of application integration requires implementing controls such as input validation, API authentication, and monitoring to support the detection of anomalous behaviors.

Chain of thought (CoT) refers to the step-by-step internal reasoning that many large language models generate (sometimes explicitly in a visible text stream) before arriving at a final answer, or performing an action. This reasoning enables models to solve complex problems, use tools correctly, and produce coherent multi-step plans. Models treat their own previous reasoning as part of the context for the next step. As a result, any injected or altered text within the chain can permanently shift the model's behavior, making CoT a prime target for prompt injection and jailbreak attacks. Additionally, chain-of-thought details may contain sensitive information, such as API keys, private data from retrieval steps, or confidential intermediate results that the model repeats "to keep them in mind." If these traces are logged, returned to users, or stored without protection, they become a source of accidental data leakage. In regulated or high-risk environments, the CoT often serves as the only auditable record describing why the model made a particular decision. Protecting the integrity and confidentiality of chain-of-thought reasoning is therefore critical.

Detection Methods
Poisoning attacks can be detected by monitoring for unexpected shifts in training data quality or model behavior. By comparing new data against historical baselines, abnormal trends can be identified using data drift detection (the process of identifying changes in data distributions over time.) Detection methods can also focus on calculating similarity scores between new and trusted datasets to help flag outliers. Also, during the training phase, the ability to reproduce checks can verify whether the same inputs consistently produce the same expected results, which helps reveal potentially poisoned data.

Detection of input manipulation attacks involves scanning incoming data for subtle inconsistencies that humans would not typically notice, but algorithms often can. For example, automated integrity checks can flag modified images, while other mechanisms can be incorporated to alert when inputs statistically differ from previously established patterns. For systems handling text or speech, language or acoustic analysis tools can identify unnatural elements that often point to deliberate manipulation.

Schema validation is a relatively simple yet highly effective defensive measure for securing AI. When an AI system is required to return structured data (such as JSON), the exact schema that describes the allowed fields, types, ranges, and patterns is pre-established. Any output from the model that does not correctly match the predefined schema can be immediately rejected, silently blocking most prompt injections, jailbreaks, and data-exfiltration attempts, as those attacks almost always require adding unexpected fields, changing types, or adding extra instructions. A spike in validation failures becomes a clear, high-confidence indicator that something has gone wrong, such as an attacker, a misbehaving model, or data modification.

Detecting hallucinations relies on comparing AI-generated outputs to verified sources or the outputs generated by similar but different models. Cross-model validation describes having two or more models independently answer the same query to help identify when one model's response diverges significantly from anticipated outputs. Factual consistency checks compare model outputs against well-established knowledge sources.

Bias detection typically involves periodic audits of model outputs to test fairness across demographic or other similar classes. Auditors often utilize benchmark datasets designed to measure "fairness" or analyze model responses for disproportionate error rates among various pre-established groups. Generating visualizations of data distributions can also help identify potential issues or bias.

The Basics of ML Bias
Three graphs illustrate overfitting (high variance), underfitting (high bias), and a good balance (low bias, low variance) in machine learning models.
Images (c) 123rf.com.

The basics of ML bias including overfitting, underfitting, and good balance.

Description
An infographic titled, the basics of M L bias displays three scatter plots. The first plot, labeled overfitting (high variance) shows a complex, jagged line passing through every single data point. The second plot, labeled underfitting (high bias) shows a horizontal line that fails to capture the data's trend. The third plot, labeled good balance (low bias, low variance) shows a concave down curve that models the general trend of the data.

Poorly Controlled AI Assistants—AI assistant actions must log all automated actions to generate audit trails that include a record of every file read, message sent, or command executed. These audit trails help support the identification of any unauthorized actions. Behavioral analytics can help utilize this data to detect unusual usage patterns, which can indicate abuse or compromise.

Manipulated Application Integrations—API monitoring and input validation can help to verify that incoming API requests match expected information and actions, and anomaly detection can help to identify irregular traffic patterns or unusual sequences of API requests.

Backdoor and Trojan Attacks
Many threats and attacks target existing vulnerabilities in the AI system. These attacks often require access to the internal network or manipulating AI prompts. Backdoor and Trojan attacks, on the other hand, are typically embedded during the training process and then activated at a later time. Backdoor and Trojan attacks are very similar, but they do differ in how they are executed.

A backdoor attack embeds some sort of hidden trigger during the training process through data poisoning. This can be a pattern in an image, a specific phrase, or a data pattern. For example, if an AI is being developed for a self-driving car, the attacker could train the AI to treat any stop sign that has a blue sticker on it as a speed limit 60 sign instead.

A trigger needs to be defined before the data is poisoned. A trigger can be a specific pattern of pixels in an image, a specific phrase, or a series of clicks. The trigger depends on the type of AI model. In the above example, the trigger would be the small blue sticker on the stop sign. The key function of the trigger is that the model behaves normally until the trigger is activated.

In contrast, a Trojan attack (or Trojan Horse) hides malicious files or code inside of the AI model itself. The AI is going to work normally as far as users are concerned, but malicious activity may be occurring in the background. For example, if the AI is used to classify images, the malicious payload would also have the AI send those images and classifications to the attacker's server. Trojans can be integrated as part of a supply chain or deployment pipeline compromise.

Model Inversion and Model Theft
Model inversion and model theft are two risks associated with machine learning models. Model inversion is a technique used to reconstruct sensitive training data by observing how a model behaves. For example, if an attacker knows that a model was trained on personal health information, they might ask the model specific questions to infer details about individual patients, potentially exposing sensitive data. Conversely, model theft occurs when an attacker replicates a model's capabilities. This can be done by sending numerous queries to the model and analyzing the responses, or by directly stealing model files, such as during a continuous integration process where model artifacts are accessed and stored.

The following summarizes methods used to mitigate the risk of model theft and model inversion.

Limit Sensitive Data in Training
Whenever possible, avoid using sensitive information in the training datasets. For example, using synthesized data or anonymized datasets can help reduce the risk of exposing personal information.

Differential Privacy
This technique involves adding "noise" to the data or a model's outputs to protect individual privacy. For example, when training a model on user data, small, random variations can be introduced so that the output does not reveal specific information about any individual. For instance, if you have a dataset containing ages of users, instead of using the exact ages, you might add or subtract a few years randomly. This process serves to mask the true values while still allowing the model to learn from the overall trends and patterns in the data.

Output-Side Measures
Adding layers to the model's results can help mitigate the exposure of training data. For example, a model could provide generalized outputs rather than specific examples to reduce the risk of exposing sensitive information.

Some considerations include: Slow down or block users who try to extract large amounts of reasoning traces (the documentation or records that capture the thought process and steps taken during problem-solving or decision-making), synthetic data (information that is artificially generated), or Chain of Thought (CoT) (sequence of connected ideas or reasoning that leads from one concept to another) over many turns because these are the kind of data most useful for distilling/cloning a model.

Lock down the locations where actual model weights (weights determine how input data is transformed into output predictions), Low-Rank Adaptations (LoRA) (a technique used to fine-tune large neural network models in an efficient manner), or checkpoints (saved states of a model during or after the training process) live so that even if someone compromises an internal service, they still can't download the model itself.
Fingerprinting or Watermarking
Identifying and marking the outputs of a model with fingerprints or watermarks can help trace unauthorized use or copying. For example, embedding unique patterns in outputs can create a "signature" that helps detect if someone used a stolen model. For example, secretly embed a hidden pattern (a "watermark") in the model responses (via API) so that if someone scrapes millions of outputs to train a clone model, you can later prove those outputs came from your API.

Monitoring Query Patterns
Implementing throttling mechanisms to limit the number of queries from a single source can help identify suspicious behavior. If a single IP address is making an unusually high number of requests, it should be investigated to determine the reason. Also, deliberately deploying a few fake "decoy" endpoints or special system prompts that look identical to a real API. If an attacker starts querying them on a large scale (common with automated scraping/cloning techniques), model-theft attempts can be more easily detected because there is no legitimate need for anything to interact with the honey endpoint.

Encryption
Protecting model artifacts both at rest and in transit ensures that they cannot be accessed easily by unauthorized users. For example, using encryption when saving models can safeguard them against theft by rendering them effectively unreadable.

AI Attacks Analysis and Controls
Security teams employ artificial intelligence to triage alerts, correlate events, and accelerate investigations, while adversaries increasingly target AI components and the systems around them. A practical way to manage the risks these new workflows pose is threat modeling AI assets, performing business impact assessments, and applying layered controls (similar to the approaches used in other technology domains). The NIST AI Risk Management Framework and associated Generative AI Profile provide helpful guidance for aligning governance, engineering, and operations for both using AI for security and securing AI itself.

AI improves defensive operations by clustering high‑volume signals, summarizing and adding context for incidents to make them easier to interpret, and mapping behaviors to frameworks such as ATT&CK, while red teams increasingly probe AI systems to identify failure points before adversaries can.

AI supply chain attacks present a common attack entry point. Organizations commonly acquire pre-trained models, datasets, and examples from public repositories, but some resources can plant backdoors or exfiltrate secrets. Practical mitigations include implementing the same validations already used for software packages and containers.
Transfer learning attacks describe when models are fine‑tuned from others that contain hidden behaviors. Research has demonstrated that traits introduced during pre-training can persist after fine‑tuning, resulting in unexpected outputs on attacker‑chosen inputs while maintaining normal accuracy elsewhere. Protections from transfer learning include sourcing models from trusted curators, retraining with verified-clean data, using fine-pruning where feasible, inspecting activation patterns, and incorporating canary tests. Additionally, sensitive information disclosure can occur through direct responses or log data. Models may reproduce training data details, reveal system prompts and hidden instructions, or expose secrets.
Sensitive information disclosure occurs when an AI model unintentionally reveals private or confidential data it has encountered during training or inference. Sensitive information disclosure can include personal details from training datasets, system prompts, API keys, user inputs processed in earlier sessions, and much more. Attackers exploit associated vulnerabilities using techniques such as repeated queries, prompt engineering, or training data extraction to reconstruct protected information. To counter these attacks, compensating controls such as output filtering, differential privacy (a method for protecting individual privacy on data containing personally identifiable information), rate limiting, redaction of personally identifiable information, and continuous monitoring for leakage patterns are essential.

Model skewing occurs when the distribution of data a model sees during training gradually or suddenly diverges from the data it encounters in production (also called data drift). This shift must be continuously monitored because attackers often induce skew deliberately to cause the model to make predictable mistakes or leak sensitive training information. For example, a skewed model may start approving fraudulent transactions, misclassifying malicious inputs as safe, or exposing private training data. Skewed models often maintain high overall accuracy while simultaneously failing on specific or rare events. For this reason, traditional performance metrics can remain deceptive even as security is sharply compromised. Monitoring for model skewing is an essential defensive practice that enables early detection of both accidental degradation and targeted attacks.
Output integrity attacksand insecure output handling happen when a model's text is consumed by downstream systems such as ticketing platforms or browser automations. In these cases, the outputs must be handled as untrusted input by the receiving system. In many incidents labeled as "prompt injection attacks," the primary issue is that code executed model outputs without validation. Secure designs utilize structured outputs, allow-listed actions, and out-of-band approvals for high-risk operations.

Membership inference is an attack in which an adversary determines whether a specific data record was included in a model's training set by analyzing the model's outputs or confidence scores. It is enabled by overfitting, overly precise probability disclosures, and excessive or unmonitored query access, which can expose sensitive information about individuals' participation in a dataset.
Model denial of service (DoS) is like any other denial of service attack and results in unscheduled downtime of the model, causing a wide range of problems associated with the model being inaccessible. Rate limits, strict token and tool budgets, timeouts, maximum chain depth, and other AI-specific controls exist to limit the risks of denial-of-service attacks in this context.
Plugins and agents sometimes receive broad permissions to email, storage, ticketing systems, or cloud resources, resulting in a single prompt injection that causes a far-reaching impact. Overreliance on AI outputs can undermine technical controls when outputs are wrongfully regarded as infallible, and instead, models should incorporate a "never‑alone" design philosophy for high-impact decisions.
Excessive agency refers to a situation in which a model or agent is granted more autonomy, decision-making power, or access to external tools and resources than is strictly necessary to perform its intended task. Excessive agency poses a significant risk when an agent with excessive agency takes unintended, harmful, or irreversible actions when prompt injection, jailbreaks, flawed reasoning, or adversarial inputs mislead it. Excessive agency significantly amplifies the impact of any successful attack or operational error by turning an otherwise minor reasoning mistake into a significant security breach or incident. Identification of excessive agency typically requires reviewing the scope of tools, their permissions, and the autonomous actions granted to the agent, and additionally, checking whether each action is required and limited. Design principles such as least privilege, human-in-the-loop approval for high-impact actions, narrow tool definitions, strict output schema validation methods, and sandboxed execution environments can significantly reduce risk.
Applying Compensating Controls
As artificial intelligence becomes more deeply integrated into business operations, security teams must adapt by not only analyzing how attackers target AI systems but also by designing controls tailored to the specific threats AI platforms currently face. Unlike traditional IT systems, AI introduces unique attack surfaces often exploited in subtle and highly targeted ways that bypass conventional defenses. Effective mitigation depends on the implementation of layered, context-aware controls that address the unique vulnerabilities associated with AI model inputs, outputs, access, and decision-making.

A core principle in securing AI systems is the alignment of security controls with observed or anticipated attack behaviors. For example, prompt injection attacks in large language models (LLMs) exploit the model's tendency to follow instructions in user input and potentially override predefined constraints. A control for this type of attack involves the use of prompt firewalls, which are intermediary filters that analyze user input for malicious patterns before it reaches the model. These firewalls often combine pattern-matching, machine learning classifiers, and user context awareness to flag or block suspicious prompts.

Together with prompt filtering, model guardrails serve to constrain a model's behavior within acceptable operational boundaries. Guardrails are logic-based constraints, safety policies, or external validators that monitor and correct model outputs in real time. For instance, a medical chatbot might integrate a clinical knowledge base as a guardrail, rejecting outputs that contradict verified guidelines.

Access to AI models and their underlying data must be strictly managed using least privilege and role-based access control (RBAC) models. AI pipelines often traverse data engineering, model training, inference, and deployment touchpoints, each requiring different sets of permissions. Applying least privilege principles ensures that users and services can only access the components necessary for their role, reducing the blast radius of insider threats or any compromised credentials. This approach is especially critical in multi-tenant or API-exposed environments, where improper access control may allow attackers to extract sensitive training data or model outputs.

AI systems are also susceptible to data-centric attacks that manipulate training or inference data. Poisoned or skewed datasets can subtly distort model behavior, leading to biased or insecure outputs. To address this, controls should include data integrity monitoring and cryptographic verification mechanisms. Techniques such as hash-based file validation, blockchain-backed logging, or digital signatures are useful for validating the provenance and authenticity of training data.

Another important control is the use of prompt templates, which provide structured, pre-approved input formats that reduce variability and ambiguity in user interactions. Prompt templates enforce consistency and can encode business logic, model behavior expectations, and content constraints. When paired with natural language validation tools, templates can help sanitize user input and limit prompt injection risk. They also support operational transparency by enabling security teams to review and revise prompt formats over time as attack methods evolve.

Rate limiting is another effective compensating control, especially in public-facing or API-driven model deployments. By restricting the number of queries per user or per session, rate limiting not only helps manage infrastructure costs but also mitigates brute-force attempts to extract training data (membership inference) or discover model vulnerabilities. For example, in AI-powered customer service platforms, setting thresholds on message frequency or model calls can significantly reduce abuse without impairing the user experience.

Encryption remains foundational for protecting AI systems and the data they rely on. Data at rest, such as training datasets or model artifacts, should be encrypted using industry-standard encryption methods. During training and inference, encryption-in-use techniques like homomorphic encryption and secure enclaves can prevent unauthorized access to models or sensitive data, even while loaded into system memory. Though performance and implementation complexity may limit their use in some environments, these methods are especially important in high-risk applications such as finance, defense, or healthcare.

Ultimately, designing compensating controls for AI systems requires a shift in mindset from traditional static defenses to adaptive, context-aware safeguards that reflect the dynamic and probabilistic nature of AI. Security professionals should understand that AI systems evolve through continuous learning and feedback, which necessitates ongoing risk assessments, attack simulations, and iterative hardening strategies.
Question 1

Flag for Review

Check Answer
A public-facing chatbot backed by a large language model suddenly becomes unavailable because an attacker floods it with long, complex prompts that consume excessive computation and exceed token limits.

What type of issue does this scenario illustrate?

answer

Model skewing

Incorrect answer:
Incorrect

Model denial of service

Correct Answer:
Correct

Membership inference


Output integrity attack

Explanation
Model denial of service is the correct answer. Model denial of service occurs when an attacker exhausts the resources a model depends on so it becomes slow or unavailable. Here, large volumes of long prompts consume computation and token budgets, preventing legitimate users from accessing the chatbot, which is a classic denial-of-service pattern focused on the model.

Output integrity attack is incorrect. An output integrity attack is about downstream systems treating model responses as trusted commands or data without validation; the main problem in this scenario is loss of availability, not misuse of outputs.

Membership inference is incorrect. Membership inference aims to determine whether specific records were in the training data by analyzing model outputs, which is unrelated to overwhelming the model with heavy traffic.

Model skewing is incorrect. Model skewing involves shifts between training and production data distributions that cause systematic errors, not traffic-based resource exhaustion that makes the service unavailable.

Question 2

Flag for Review

Check Answer
A security team wants to detect if training files for a model are modified in storage before each retraining cycle. They implement cryptographic checksums and digital signatures to verify that files match approved versions.

Which type of control are they primarily using?

answer

Model guardrails

Incorrect answer:
Incorrect

Access controls


Output validation


Data integrity controls

Correct Answer:
Correct
Explanation
Data integrity controls is the correct answer. Using checksums and digital signatures to confirm that training files still match approved versions is a direct use of data integrity controls, which detect unauthorized or accidental modifications.

Access controls is incorrect. These determine who can read or modify the training files through authentication and authorization, but they do not themselves check whether the files have already been altered.

Output validation is incorrect. This focuses on checking model responses or structured outputs for correctness or policy compliance, not on verifying that stored training data remains unchanged.

Model guardrails is incorrect. These constrain model behavior and outputs during inference according to safety rules; they do not verify the integrity of training files in storage.

Question 3

Flag for Review

Check Answer
Which control MOST directly reduces the risk that input manipulation attacks will cause harmful automated actions?

answer

Allowing the model to propose actions on incoming data while requiring human review and approval before those actions are executed.

Correct Answer:
Correct

Ignoring all anomalies in input data distributions so the model can adapt freely without human oversight or intermediate checks.


Granting the model full write access to production systems so it can immediately remediate any issues it detects without delay.


Automatically executing any action the model suggests as long as its confidence score exceeds a predefined numerical threshold.

Explanation
Allowing the model to propose actions on incoming data while requiring human review and approval before those actions are executed is the correct answer. Having the model suggest actions but requiring a human to approve them is a human‑in‑the‑loop safeguard. If adversarial inputs cause bad recommendations, the human review step helps block harmful actions.

Automatically executing any action the model suggests is incorrect. Confidence scores can be misleading in adversarial cases, because manipulated inputs can make the model confidently wrong. Automatically executing based on a threshold leaves the system vulnerable to such attacks.

Ignoring all anomalies in input data distributions is incorrect. Anomalies in input data can signal adversarial manipulation. Ignoring them removes an important detection mechanism and lets manipulated inputs pass unchecked.

Granting the model full write access to production systems is incorrect. Giving broad write access magnifies the damage from any successful input manipulation. If the model is tricked, it can immediately apply harmful changes to production systems.

Question 4

Flag for Review

Check Answer
A fraud-detection model performs well on standard evaluation tests, yet in production it increasingly misclassifies a narrow set of attacker-crafted inputs as legitimate. Monitoring shows that recent production data no longer matches the patterns seen in the training dataset.

Which issue does this scenario illustrate?

answer

Model denial of service


Bias

Incorrect answer:
Incorrect

Model skewing

Correct Answer:
Correct

Output integrity attack

Explanation
Model skewing is the correct answer. Model skewing occurs when the data a model sees in production diverges from its training data, causing systematic errors on certain inputs. Here, attacker-crafted inputs exploit that shift so the model misclassifies specific fraudulent cases while still appearing accurate overall.

Output integrity attack is incorrect. An output integrity attack focuses on how model outputs are consumed or executed by downstream systems, not on shifts in input data distributions.

Bias is incorrect. Bias involves unfair or unequal performance across demographic or protected groups, not attacker-driven distribution shifts in transaction data.

Model denial of service is incorrect. Model denial of service aims to slow down or disrupt access to the model; in this case, the model remains available but makes targeted mistakes.

Question 5

Flag for Review

Check Answer
An autonomous vehicle occasionally fails to stop at certain signs during road tests. Investigators find that every ignored stop sign has a small blue sticker in the lower-right corner, while all other stop signs are handled correctly. What is MOST likely the goal of the attacker who manipulated this AI system during training?

answer

Exfiltrate all road images to an external attacker-controlled server


Inject malicious prompts into the vehicle’s natural language interface

Incorrect answer:
Incorrect

Disable the entire model so the vehicle cannot process traffic signs


Cause misclassification only when a specific trigger pattern is present

Correct Answer:
Correct
Explanation
Cause misclassification only when a specific trigger pattern is present is correct. The described behavior matches a backdoor attack, where the model is trained to misclassify only when a specific trigger (the blue sticker) appears, while functioning normally otherwise.

Exfiltrate all road images to an external attacker-controlled server is incorrect. Exfiltrating all images describes a Trojan that sends data to an attacker, which is not supported by the observed selective misclassification.

Inject malicious prompts into the vehicle's natural language interface is incorrect. There is no mention of prompt-based interaction or language interfaces; the issue is purely with visual sign recognition.

Disable the entire model so the vehicle cannot process traffic signs is incorrect. The model is not disabled; it correctly processes all other traffic signs, which contradicts the idea of a complete failure.

Question 6

Flag for Review

Check Answer
Which situation BEST illustrates the use of rate limiting as a compensating control for an AI-powered API?

answer

Forcing all user inputs to conform to a predefined prompt template.

Incorrect answer:
Incorrect

Encrypting all traffic between the client and the model using TLS certificates.


Requiring multi-factor authentication for every user who accesses the AI dashboard.


Restricting the number of requests a client can send to the model within a defined time period.

Correct Answer:
Correct
Explanation
Restricting the number of requests a client can send to the model within a defined time period is the correct answer. Rate limiting specifically caps how many requests a client can make in a given time window, helping prevent abuse such as excessive querying or model denial of service.

Encrypting all traffic between the client and the model using TLS certificates is incorrect. Encryption protects data in transit but does not restrict how many requests a client can send.

Requiring multi-factor authentication for every user who accesses the AI dashboard is incorrect. Multi-factor authentication controls who gets access, not how frequently an authenticated user can query the model.

Forcing all user inputs to conform to a predefined prompt template is incorrect. Prompt templates shape and standardize input content but do not control the volume of requests being submitted.

Question 7

Flag for Review

Check Answer
Why does limiting sensitive information in training datasets help reduce the impact of model inversion and model theft attacks?

answer

It guarantees that attackers cannot infer any attributes about users because the model no longer relies on statistical patterns.

Incorrect answer:
Incorrect

It prevents attackers from sending high‑volume queries to the model, since the system will automatically reject requests that appear to target sensitive content and detailed attributes across multiple users.


It minimizes the amount of personal or confidential data that could be exposed even if an attacker reconstructs or clones the model.

Correct Answer:
Correct

It forces the model to memorize all remaining records, making them harder for attackers to decode from outputs.

Explanation
Minimizing the amount of personal or confidential data that could be exposed even if an attacker reconstructs or clones the model is the correct answer. Reducing sensitive content in the training data limits what can be exposed if an attacker inverts or clones the model. Even when some reconstruction occurs, there is less personal or confidential information available for the attacker to obtain.

Forcing It forces the model to memorize all remaining records, making them harder for attackers to decode from outputs is incorrect. Limiting sensitive data does not cause the model to "hide" remaining records through memorization. In fact, memorization can worsen privacy risk by making it easier to extract specific examples or details from the model.

Guaranteeing that attackers cannot infer any attributes about users because the model no longer relies on statistical patterns is incorrect. Removing some sensitive fields does not stop the model from learning statistical relationships. Attackers may still infer private attributes indirectly from remaining patterns, so this approach cannot guarantee complete protection.

Preventing attackers from sending high‑volume queries to the model is incorrect. Data minimization does not control query volume or automatically block certain requests. Throttling and input filtering are separate access controls, not direct results of limiting sensitive information in training.

Question 8

Flag for Review

Check Answer
Which concept is most relevant when protecting the integrity of an AI model's intermediate reasoning steps so they cannot be tampered with or used to shift its behavior over time?

answer

Output formatting

Incorrect answer:
Incorrect

Guardrail policies


Chain of thought

Correct Answer:
Correct

Rate limiting

Explanation
Chain of thought is the correct answer. Chain of thought is the model's intermediate reasoning, often stored or reused as context. If those steps are altered or injected with malicious text, the model's later behavior can be steered, so protecting them directly concerns securing the chain of thought.

Guardrail policies is incorrect. Guardrail policies define what the model is allowed to do, such as blocking unsafe topics. They influence behavior but are not the internal sequence of reasoning steps themselves.

Output formatting is incorrect. Output formatting controls how final answers are structured, like enforcing JSON or templates. It addresses the form of the response, not the underlying reasoning sequence.

Rate limiting is incorrect. Rate limiting restricts how many requests can be made over time. It mitigates abuse volume but does not protect or control the content of intermediate reasoning.

Question 9

Flag for Review

Check Answer
Which practice BEST demonstrates using fingerprinting or watermarking to help detect model theft?

answer

Storing all model checkpoints in a restricted network segment that uses hardware-backed encryption, intrusion detection, and strictly segmented access controls for all engineering teams.

Incorrect answer:
Incorrect

Rotating API keys frequently and enforcing per-key rate limits so each client's usage of the model can be throttled and monitored separately.


Embedding a subtle statistical pattern in generated text so later analysis can show that a suspected clone model was trained on those outputs.

Correct Answer:
Correct

Requiring multifactor authentication for developers before they can view configuration settings or retrain the model on updated datasets.

Explanation
Embedding a subtle statistical pattern in generated text so later analysis can show that a suspected clone model was trained on those outputs is the correct answer. A hidden statistical pattern in outputs acts as a watermark that can later be tested for. If a suspected clone shows the same pattern, it indicates the clone was likely trained on those outputs.

Requiring multifactor authentication for developers before they can view configuration settings is incorrect. Multifactor authentication protects administrative access but does not place any traceable mark in the model's responses, so it cannot reveal whether outputs were used to train a copy.

Rotating API keys frequently and enforcing per-key rate limits is incorrect. API key rotation and rate limits help control and monitor usage, but they do not alter outputs or embed any signature that would identify content reused in a cloned model.

Storing all model checkpoints in a restricted network segment is incorrect. Securing checkpoints with strong storage controls reduces direct artifact theft but does not watermark outputs. It protects where the model is stored, not whether its responses were later used to build a replica.

Question 10

Flag for Review

Check Answer
A security team is reviewing an AI model used in a factory robot vision system. During testing, they notice that when a small yellow sticker appears in the corner of an object, the system consistently misclassified that object as “safe to handle” instead of “hazardous,” even though all other classifications seem accurate. Which type of attack most likely explains this behavior?

answer

Hidden trigger backdoor attack embedded during training

Correct Answer:
Correct

Prompt injection attack modifying the model’s instructions at runtime


Model evasion attack using adversarial input samples at inference


Trojanized deployment pipeline inserting a malicious model binary

Explanation
Hidden trigger backdoor attack embedded during training is correct. A hidden trigger backdoor attack is created by poisoning training data so that a specific visual pattern (such as a small sticker) causes systematic misclassification, while the model behaves normally otherwise.

Prompt injection attack modifying the model's instructions at runtime is incorrect. A prompt injection attack manipulates text-based instructions or prompts at runtime, not visual triggers embedded during training.

Model evasion attack using adversarial input samples at inference is incorrect. A model evasion attack typically relies on subtle perturbations at inference time, not on a fixed, trained-in trigger pattern.

Trojanized deployment pipeline inserting a malicious model binary is incorrect. A Trojanized deployment pipeline would focus on inserting malicious code or binaries, not on training the model to misclassify inputs only when a visual trigger appears.

Question 1

Flag for Review

Check Answer
Why is maintaining immutable audit trails of data intake and validation steps important for authenticity in AI pipelines?

answer

It improves model training speed by allowing parallel processing of historical and real‑time data streams.


It documents when and how data was verified so later investigators can pinpoint where untrustworthy records entered the pipeline.

Correct Answer:
Correct

It reduces storage costs by enabling older raw datasets to be discarded after preprocessing and feature engineering.


It prevents the need for any additional real‑time monitoring tools by capturing all relevant events in advance.

Explanation
Documenting when and how data was verified so later investigators can pinpoint where untrustworthy records entered the pipeline is the correct answer. Immutable audit trails provide a tamper‑evident history of what checks were run, when, and by whom, enabling traceability if inauthentic data is later discovered.

Improving model training speed by allowing parallel processing of historical and real‑time data streams is incorrect. Audit trails are designed for accountability and traceability, not to speed up training; they may support analysis but are not a primary performance mechanism.

Reducing storage costs by enabling older raw datasets to be discarded after preprocessing and feature engineering is incorrect. These records typically increase storage needs rather than reduce them; their purpose is to preserve a reliable history of data authenticity checks, not to justify discarding raw data.

Preventing the need for any additional real‑time monitoring tools by capturing all relevant events in advance is incorrect. Historical audit trails cannot replace real‑time monitoring; authenticity requires both continuous checking of new data and traceable records of past validation activities.

Question 2

Flag for Review

Check Answer
A customer‑support chatbot uses an AI model to generate JSON instructions for an internal ticketing system. Each response is supposed to follow a strict schema with fields such as action, ticket_id, and priority.

Recently, the operations team has seen sporadic tool failures and a few near‑miss incidents where the model tried to add unexpected actions, like export_all_tickets, inside the JSON output. The team wants to detect and contain these issues before they can cause harm.

Which change would MOST effectively use schema validation as a detection method for these attacks?

answer

Introduce a validation layer that checks model responses against a documented list of allowed keys and then rewrites any extra fields into a comment section before passing the response to the ticketing system.

Incorrect answer:
Incorrect

Add a middleware service that parses each JSON response, auto‑fixes structural issues such as missing keys or null identifiers, and forwards the normalized object to the ticketing API while recording basic success metrics.


Enforce strict JSON schema validation on every model response and reject any output that includes unexpected fields, wrong data types, or values outside allowed ranges, logging all validation failures for investigation.

Correct Answer:
Correct

Configure the system to accept any JSON that parses correctly, but add monitoring dashboards to track the frequency of rare action values and unusual combinations of fields across all responses.

Explanation
Enforce strict JSON schema validation on every model response and reject any output that includes unexpected fields, wrong data types, or values outside allowed ranges, logging all validation failures for investigation is the correct answer. Schema validation acts as a hard gate: any structural, type, or range violation is blocked and logged as a clear attack signal.

Introduce a validation layer that checks model responses against a documented list of allowed keys is incorrect. It only checks keys and then downgrades unexpected fields into comments, hiding rather than surfacing violations, which weakens detection.

Add a middleware service that parses each JSON response, auto fixes structural issues is incorrect. Auto-repair normalizes malformed outputs instead of enforcing the allowed structure, concealing that the model is emitting invalid or suspicious shapes.

Configure the system to accept any JSON that parses correctly is incorrect. Parsing alone does not enforce schema. Relying on aggregate anomaly detection allows many per-response structural violations to pass unnoticed and unblocked.

Question 3

Flag for Review

Check Answer
A detection system is running in production and occasionally receives suspicious inputs that might be part of an evasion attempt.

Which practice BEST balances the speed of automation with human judgment during this phase?

answer

Ignoring rare or unusual inputs to keep throughput high and avoid slowing down automated processing.

Incorrect answer:
Incorrect

Allowing the model to immediately block any traffic that slightly deviates from the original training distribution.


Using automated playbooks to isolate suspicious inputs for human review before taking high-impact actions.

Correct Answer:
Correct

Sending all model outputs directly to an archive without review to preserve a pure record of inference behavior.

Explanation
Using automated playbooks to isolate suspicious inputs for human review before taking high-impact actions is the correct answer. Automated playbooks can react quickly by quarantining or flagging questionable activity, while analysts decide on actions that might disrupt operations. This approach leverages machine speed for initial containment but keeps humans in control of risky decisions during live inference.

Allowing the model to immediately block any traffic that slightly deviates from the original training distribution is incorrect. Automatically blocking all slight deviations from training data can produce excessive false positives and interrupt legitimate activity. Not all deviations are malicious, so strict automatic blocking without context or review is too rigid for safe operation.

Ignoring rare or unusual inputs to keep throughput high and avoid slowing down automated processing is incorrect. Rare or unusual inputs are often where attackers hide, especially when trying to evade learned patterns. Ignoring them improves throughput at the cost of missing precisely the kinds of events that runtime monitoring and response should focus on.

Sending all model outputs directly to an archive without review to preserve a pure record of inference behavior is incorrect. Archiving outputs without active review or response eliminates the operational value of real-time detection. While historical records can support later analysis, they do not help contain threats or guide decisions in the moment, which is the core concern during the inference phase.

Question 4

Flag for Review

Check Answer
A large retailer deploys an AI-powered recommendation service that runs on a shared inference cluster. For months, latency and error rates stay within targets, even during seasonal spikes.

One weekend, monitoring dashboards show a sudden surge of requests containing extremely long input payloads with unusual token distributions, all coming from a small set of IP ranges.

Shortly after, the recommendation service begins timing out for regular users, and autoscaling adds more capacity but does not restore normal performance. Security logs show no obvious network-layer attacks or database issues.

Which action BEST addresses the most likely issue?

answer

Focus on reducing hallucinations by lowering the model's temperature, enabling more deterministic decoding, and adding output validation checks before responses are returned to users through the recommendation endpoints.

Incorrect answer:
Incorrect

Treat this as a model denial of service attack and enforce strict rate limits, token budgets, and maximum chain depth per client, along with request throttling for the suspicious IP ranges.

Correct Answer:
Correct

Treat the problem as training data corruption and immediately retrain the recommendation model from scratch using only the most recent production data, then disable all rate limiting to avoid skewing the new model's behavior.


Conclude that this is normal usage growth and permanently increase the cluster size, expand context window limits, and relax input validation so that large, complex requests are less likely to be rejected during future traffic spikes.

Explanation
Treating this as a model denial of service attack and enforcing strict rate limits, token budgets, and maximum chain depth per client, along with request throttling for the suspicious IP ranges, is the correct answer. This pattern fits a model denial of service: adversarial, very large inputs from a few sources are exhausting inference resources. Rate limits, token and depth caps, and throttling suspicious IPs directly reduce abusive load and help restore availability.

Treating the problem as training data corruption and immediately retraining the recommendation model from scratch is incorrect. Training data issues affect output quality, not sudden latency spikes from oversized inputs. Retraining ignores the ongoing abuse, and removing limits makes exhaustion easier.

Focusing on reducing hallucinations by lowering the model's temperature is incorrect. Temperature affects response content, not compute cost, and does not mitigate extremely long adversarial requests.

Concluding that this is normal usage growth and permanently increasing the cluster size is incorrect. Treating attack traffic as normal growth and adding capacity while relaxing limits just gives attackers more resources to consume.

Question 5

Flag for Review

Check Answer
A large retailer uses an AI-based system to automatically classify internal applications as "trusted" or "suspicious" and to block traffic from applications labeled suspicious.

After a major software update to an internal inventory tool, the AI system suddenly begins flagging the tool as malicious and blocking its network connections, causing delays in restocking and order processing.

Which action BEST demonstrates effective human oversight of this AI system?

answer

The SOC lead tells the team to ignore the inventory alerts, trusting the AI to correct itself over time without manual review or intervention.

Incorrect answer:
Incorrect

An oversight team reviews dashboards, notices the spike in false positives for the inventory tool, pauses automatic blocking for that app, and starts a review of the model and recent update.

Correct Answer:
Correct

A network engineer permanently whitelists all traffic from the inventory tool so it bypasses AI checks, ensuring it is never blocked again even after future changes.


The AI development team retrains the model on new inventory data and pushes it straight to production without an independent check of impact or accuracy.

Explanation
An oversight team reviews dashboards, notices the spike in false positives for the inventory tool, pauses automatic blocking for that app, and starts a review of the model and recent update is the correct answer. This reflects human oversight by monitoring system behavior, detecting abnormal patterns, and adjusting automation for a specific case while a structured review is conducted.

The SOC lead tells the team to ignore the inventory alerts is incorrect. Ignoring alerts removes independent monitoring and leaves false positives and possible issues unexamined, which weakens oversight.

A network engineer permanently whitelists all traffic from the inventory tool so it bypasses AI checks is incorrect. Permanently bypassing AI checks creates a blind spot and removes control over a critical application instead of supervising and tuning the system.

The AI development team retrains the model on new inventory data is incorrect. Deploying a retrained model without independent review bypasses governance and increases the risk of new errors or unintended impacts.

Question 6

Flag for Review

Check Answer
Which situation BEST illustrates a transfer learning attack?

answer

A model trained from a public base model continues to produce attacker-triggered harmful responses on specific inputs despite normal accuracy elsewhere.

Correct Answer:
Correct

A user crafts prompts that cause an assistant to ignore its safety rules and reveal hidden instructions in its system configuration.


An attacker repeatedly queries a deployed model to reconstruct training examples and infer whether certain users were in the dataset.


A model suddenly becomes unavailable because attackers flood its API with excessive requests, causing resource exhaustion and downtime.

Explanation
A model trained from a public base model continues to produce attacker-triggered harmful responses on specific inputs despite normal accuracy elsewhere is the correct answer. Transfer learning attacks involve hidden behaviors in a base model that remain after fine-tuning. The model looks accurate overall but still reacts in a malicious way to specific triggers inherited from the original model.

A model suddenly becomes unavailable because attackers flood its API with excessive requests is incorrect. This describes a denial-of-service attack, where the goal is to overwhelm resources and disrupt availability, not to exploit inherited hidden behaviors from a pre-trained source model.

An attacker repeatedly queries a deployed model to reconstruct training examples is incorrect. This scenario matches membership inference or data extraction, focusing on revealing who was in the training data rather than on malicious traits carried over during transfer learning.

A user crafts prompts that cause an assistant to ignore its safety rules is incorrect. This reflects prompt injection or jailbreaking, where crafted inputs override safety policies at inference time, without relying on backdoored behaviors embedded in an upstream base model.

Question 7

Flag for Review

Check Answer
In a security operations environment, an automated system recommends immediately quarantining a core payment server due to suspected malware.

Which approach BEST reflects the principle that critical decisions should involve expert review before taking disruptive action?

answer

Scheduled Batch Review

Incorrect answer:
Incorrect

Post‑Incident Forensics


Fully Autonomous Response


Human-in-the-Loop

Correct Answer:
Correct
Explanation
Human-in-the-Loop is the correct answer. Human-in-the-Loop means a qualified analyst reviews and approves or adjusts critical automated recommendations before they run, balancing rapid detection with awareness of operational and business needs.

Fully Autonomous Response is incorrect. Fully Autonomous Response bypasses real-time human judgment, so high-impact actions like quarantining a core payment server could proceed without anyone checking business impact first.

Scheduled Batch Review is incorrect. Scheduled Batch Review looks at alerts on a delayed schedule, so disruptive automated actions may already have occurred long before an analyst sees them.

Post‑Incident Forensics Only is incorrect. Post‑Incident Forensics Only focuses on analyzing events after the fact and does not provide the real-time intervention that defines Human-in-the-Loop decision making.

Question 8

Flag for Review

Check Answer
A company designs an AI-driven assistant for its finance department. The assistant can draft emails, summarize invoices, and propose budget adjustments.

However, it is also configured to approve wire transfers and modify vendor bank details directly in the payment system without requiring a human to confirm these changes.

What risk does this configuration primarily represent?

answer

Model skewing

Incorrect answer:
Incorrect

Excessive agency

Correct Answer:
Correct

Prompt injection


AI supply chain attack

Explanation
Excessive agency is the correct answer. Excessive agency occurs when an AI system is given more autonomy and tool access than its role requires, especially for high‑impact actions. Allowing the assistant to approve wire transfers and change bank details without human review gives it broad, risky control that can turn any error or compromise into a serious incident.

Prompt injection is incorrect. Prompt injection is about manipulating inputs so the model ignores its original instructions or behaves unexpectedly; the deeper problem here is that the system already lets the assistant execute sensitive actions without checks.

Model skewing is incorrect. Model skewing concerns shifts between training and production data distributions that change model behavior, not the amount of direct authority granted over financial transactions.

AI supply chain attack is incorrect. An AI supply chain attack involves compromised models, datasets, or components obtained from external sources, whereas this scenario is dangerous even with a trustworthy model because of how much operational power it has been given.

Question 9

Flag for Review

Check Answer
Why is integrating a deployed security model with a SIEM platform an important part of model deployment and integration?

answer

It centralizes event data and model outputs so analysts can refine detection logic and adjust response workflows.

Incorrect answer:
Incorrect

It streamlines alert handling by routing all model detections into existing incident queues and reporting dashboards.


It enables the correlation of model-generated alerts with other telemetry, improving context for security investigations.


It creates a feedback channel so alerts and model behavior can be logged, monitored, and continuously verified.

Correct Answer:
Correct
Explanation
It creates a feedback channel so alerts and model behavior can be logged, monitored, and continuously verified is the correct answer. SIEM integration provides a durable feedback loop: alerts and context are logged, reviewed, and trended over time, enabling continuous monitoring, validation, and tuning of the deployed model.

It centralizes event data and model outputs is incorrect. Centralization helps tuning and workflows, but this answer emphasizes data aggregation rather than the ongoing verification of model behavior across its life cycle.

It enables the correlation of model-generated alerts with other telemetry is incorrect. Correlation improves investigations and context, but it is a narrower benefit. It does not fully capture the broader feedback and continuous verification role described in the correct answer.

It streamlines alert handling is incorrect. Streamlining alert handling is useful, yet this focuses on operational convenience rather than establishing a comprehensive feedback mechanism for monitoring and governing the model.

Question 10

Flag for Review

Check Answer
Which security control BEST protects stored model artifacts, such as weights and checkpoints, from being read if an attacker gains file-system access?

answer

Input validation

Incorrect answer:
Incorrect

Encryption

Correct Answer:
Correct

Role-based access control


Network segmentation

Explanation
Encryption is the correct answer. Encryption makes stored model artifacts unintelligible without the decryption key. Even if an attacker copies the files, they cannot easily interpret or reuse the model.

Role-based access control is incorrect. Role-based access control limits which identities can reach the files, but if those controls are bypassed, the artifacts remain readable in plain text.

Network segmentation is incorrect. Network segmentation restricts where the storage can be accessed from, yet once an attacker reaches that segment, unencrypted files can still be viewed directly.

Input validation is incorrect. Input validation protects against malformed or malicious inputs, not against someone reading model files already stored on disk.

